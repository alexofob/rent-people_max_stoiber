import React from 'react';

const styles = {
  rectangle: {
    height: 100,
    width: '100%',
    backgroundColor: 'red',
  },
};
const CallToAction = () => (
  <section>
    <div style={styles.rectangle} />
  </section>
);

export default CallToAction;
