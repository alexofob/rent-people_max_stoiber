import React from 'react';

const styles = {
  rectangle: {
    height: 100,
    weight: '100%',
  },
};
const HomeNavBar = () => (
  <section>
    <div style={styles.rectangle} />
  </section>
);

export default HomeNavBar;
